<%= form_for(@congestion) do |f| %>
  <% if @congestion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@congestion.errors.count, "error") %> prohibited this congestion from being saved:</h2>

      <ul>
      <% @congestion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.number_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
