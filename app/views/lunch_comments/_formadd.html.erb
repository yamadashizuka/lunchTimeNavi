<%= form_for(@lunch_comment) do |f| %>
  <% if @lunch_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lunch_comment.errors.count, "error") %> prohibited this lunch_comment from being saved:</h2>

      <ul>
      <% @lunch_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :lunch_id %>
<TABLE>
<td class="lunch_comment">
 <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text , {:maxlength =>255, :size=>"40x10"}%>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rating_id %><br />
    <%= f.collection_select( :rating_id, @ratings, :id, :name, :include_blank => true ) %><br />
  </div>
</td>
</table>
  <div class="actions">
    <%= f.submit "tesut" %>
  </div>
<% end %>
</TABLE>