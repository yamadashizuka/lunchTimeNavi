<%= form_for(@lunch_comment) do |f| %>
  <% if @lunch_comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@lunch_comment.errors.count, "error") %> prohibited this lunch_comment from being saved:</h2>

      <ul>
      <% @lunch_comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :lunch_id %>
<TABLE BORDER=0 CELLPADDING=10 BGCOLOR="ORANGE"><TR><TD VALIGN="TOP">
 <div class="field">
    <%= f.label :text %><br />
    <%= f.text_area :text , {:maxlength =>255, :size=>"40x10"}%>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :rating %><br />
    <%= f.collection_select( :rating_id, @ratings, :id, :name ) %><br />
  </div>
  <div class="actions">
    <%= f.submit "登録" %>
  </div>
<% end %>
</TABLE>